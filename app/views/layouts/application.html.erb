<!DOCTYPE html>
<html>
  <head>
    <title>YourApp</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <!-- quick styles; swap for Tailwind/Bootstrap later -->
    <style>
      .navbar { display:flex; justify-content:space-between; align-items:center; padding:.75rem 1rem; border-bottom:1px solid #e5e7eb; }
      .brand { font-weight:600; text-decoration:none; margin-right:1rem; }
      .nav-link { margin-right:.75rem; text-decoration:none; color:#111827; }
      .nav-right, .nav-left { display:flex; align-items:center; gap:.5rem; }
      .btn { padding:.45rem .8rem; border:1px solid #d1d5db; border-radius:.5rem; text-decoration:none; display:inline-block; }
      .btn:hover { background:#f9fafb; }
      .container { max-width: 900px; margin: 1.5rem auto; padding: 0 1rem; }
      .flash { margin: .75rem 1rem; padding:.5rem .75rem; border:1px solid #e5e7eb; border-radius:.5rem; }
    </style>
  </head>

  <body>
    <%= render "shared/navbar" %>

    <% flash.each do |type, msg| %>
      <div class="flash"><%= msg %></div>
    <% end %>

    <main class="container">
      <%= yield %>
    </main>
  </body>
</html>
